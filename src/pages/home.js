import React from 'react';

import CoverVideo from '../components/UI-components/coverVideos';

const home = (props)=>{
    return (
        <section>
            <CoverVideo/>
        </section>
    );
}

export default home;