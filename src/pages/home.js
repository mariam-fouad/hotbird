import React from 'react';

import CoverVideo from '../components/UI-components/coverVideos';

const home = (props)=>{
    return (
        <section>
            <h1>Home</h1>
            <CoverVideo/>
        </section>
    );
}

export default home;